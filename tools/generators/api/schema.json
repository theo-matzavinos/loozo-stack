{
  "$schema": "http://json-schema.org/schema",
  "id": "api",
  "type": "object",
  "cli": "nx",
  "properties": {
    "domain": {
      "type": "string",
      "description": "Domain name",
      "x-prompt": "To which domain does this api library belong?",
      "enum": [
        "account",
        "auth",
        "contracts",
        "custom-reports",
        "delivery-note",
        "import",
        "my-advisors",
        "my-entities",
        "navigation",
        "organization",
        "projects",
        "reporting",
        "requests",
        "segments",
        "seniority-levels",
        "transactions",
        "users",
        "dashboard"
      ]
    }
  },
  "required": ["domain"]
}
